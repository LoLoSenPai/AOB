Original prompt: Build a playable solo Age of Empires-like web RTS/village builder prototype named Age Of Blockchains with Phaser 3, TypeScript, and Vite.

## 2026-04-24

- Current milestone: vertical slice solo with clean village loop, stable renderer data manifests, coherent map, and no Sunnyside world props.
- Constraint: Sunnyside remains allowed only for character animation sheets.
- Validation target: build, core tests, Playwright playtest, and inspected screenshots after each meaningful visual pass.
- Implemented: typed visual manifest, declarative map layout, construction ground stamping, terrain render pass with larger stamps, native cursors, debug hooks, command dock building icons, and cleanup of unused Sunnyside enemy/prop animation preload.
- Verified: `npm run build`, `npm run test`, `npm run playtest`, plus screenshots in `test-results/vertical-slice-*.png`.
- UI pass: subtler unit/resource/building selection, hover state separated from selection, less opaque building placement preview, full-health building bars hidden on map, and RTS-style top bar.
- Last-assets pass: runtime grass now uses `public/last-assets/grass-256x256.png` to keep terrain light and exact-sized; Town Center tiers now use `hdv-t1/t2/t3.png` directly with adjusted display size and ground footprint. Verified T1/T2 screenshots in `test-results/last-assets-*.png`.
- Age visual correction: removed age-based ground footprint scaling, reduced Town Center display size, compensated HDV tier display sizes so age upgrades do not inflate the building, and removed the grey/white age foundation bars. Verified with `test-results/no-age-scale-towncenter-t1.png` and `test-results/no-age-scale-towncenter-t2.png`.
- Town Center scale/layout correction: reduced HDV tier display sizes again and moved the starter House from tile `{ x: 49, y: 58 }` to `{ x: 44, y: 56 }` to prevent visual overlap with the new HDV asset. Verified with `test-results/hdv-smaller-house-offset-t1.png` and `test-results/hdv-smaller-house-offset-t2.png`.
- Selection UI pass: bottom panel now has a portrait/icon area, selected entity metadata, DOM health bar, cleaner help text, and tier-aware building icons in command buttons. Verified with `test-results/selection-panel-towncenter.png` and `test-results/selection-panel-villagers.png`.
- Last-assets icon pass: added alpha-cropped runtime copies in `public/last-assets/runtime`, switched HUD resource/population icons and wall command icon to those runtime images, and preloaded palisade/gate assets for later wall rendering. Verified with `npm run build`, `npm run test`, `npm run playtest`, `test-results/runtime-icons-baseline.png`, and `test-results/runtime-icons-build-menu.png`.
- Asset note: `wall-palisade-horizontal.png` and `gate-palisade.png` are usable for UI and later gate work. `wall-palisade-vertical-1.png` is not production-ready for the map; `wall-palisade-vertical-2.png` is visually better but does not connect cleanly enough to replace in-world wall segments yet.
- Wall pass: palisade sprites now render as grouped visual chunks spanning up to 4 gameplay tiles instead of one sprite per tile, using the latest `public/last-assets/runtime` wall assets. Wall construction assignment now picks reachable approach tiles, treats that approach tile as valid build range, and chains villagers to the next unfinished wall segment. Added a core regression test for multi-villager wall-line construction. Verified with `npm run build`, `npm run test`, `npm run playtest`, and screenshots `test-results/palisade-wall-line-before-build-fixed.png`, `test-results/palisade-wall-line-after-build-fixed.png`, `test-results/palisade-wall-line-after-build-counted.png`.
- Wall correction pass: the 4-tile rule now lives in gameplay, not only in rendering. A palisade segment now occupies/costs/builds as one 4-tile wall footprint, and dragging a wall line previews transparent palisade sprites instead of green tile squares. Wall asset URLs are versioned to avoid browser-cache mixing old/new sprites. Verified with `npm run build`, `npm run test`, `npm run playtest`, and screenshots `test-results/wall-drag-preview-ghost.png`, `test-results/wall-drag-placed-segments.png`, `test-results/wall-drag-built-segments.png`; debug state showed `walls: 4`, `completedWalls: 4` for the test line.
- Wall UX follow-up: reduced 4-tile palisade sprite display sizes so dragged horizontal segments do not overlap heavily, narrowed vertical palisades to preserve their original look, made right-click cancel active placement, and kept wall placement mode active after a line is placed so the player can draw the next line without reopening Build. Verified with `npm run build`, `npm run test`, `npm run playtest`, the develop-web-game Playwright smoke client, and screenshots `test-results/wall-chain-horizontal-preview.png`, `test-results/wall-chain-vertical-preview.png`, `test-results/wall-chain-after-two-lines.png`; debug state showed `walls: 7` after placing 4 horizontal + 3 vertical segments in one wall-placement session.
- Wall anchoring/hitbox pass: wall segments are now generated from the initial drag tile and direction so the first segment stays stable while the preview grows in 4-tile chunks. Wall selection now uses the displayed palisade bounds instead of the old 1x1 wall footprint, so clicking on the visible asset selects the wall. Verified with `npm run build`, `npm run test`, `npm run playtest`, screenshots `test-results/wall-anchor-drag-1.png`, `test-results/wall-anchor-drag-3.png`, `test-results/wall-anchor-drag-4.png`, `test-results/wall-hit-left-side-selected.png`, `test-results/wall-hit-right-side-selected.png`; debug state returned `selected: ["Wall"]` for clicks on both sides of the horizontal palisade asset.
- Solana map decal pass: accepted generated road and transition lots, copied stable files under `public/assets/aob-map/solana/roads` and `public/assets/aob-map/solana/terrain-transitions`, added manifest/preload keys, introduced `imageDecal` overlays, inverted grass-to-resource transition placement so the material stays inside and grass stays outside, moved road decals below the central HDV plaza decal, hid old rendered `path/dirt` chunks, restored the full stone quarry patch, and simplified/faded visible roads to avoid rectangular seams. Verified with `npm run build`, `npm run test`, `npm run playtest`, and inspected `test-results/solana-visual-pass-initial.png`, `test-results/solana-visual-pass-towncenter.png`, and `test-results/solana-visual-pass-980.png`.
- Solana decal follow-up: restored the full central crystal-ground patch inside the crystal quarry, below the grass/crystal transition decals. Verified with `npm run build`.
- Solana map cleanup: removed the added road decals and the old `path` terrain stamps from the runtime layout so village roads no longer appear in the playable viewport/minimap. Kept the central HDV plaza, quarry patches, and resource areas. Verified with `npm run build`, `npm run test`, `npm run playtest`, and inspected `test-results/solana-visual-pass-initial.png`.

## 2026-04-26

- RTS Core Loop v2: froze asset work and added guided solo objectives for 8 villagers, an economy building, Settlement age, Barracks, and 3 infantry. Objective completion is tracked in core state and announced through the message log.
- Production loop: producer buildings now support rally points through `setRallyPoint`; right-clicking terrain with a Town Center/Barracks selected stores the rally point, shows a marker, and trained units path toward it after spawning.
- HUD pass: added a compact objective panel, idle worker button, villager task summary, production queue/progress display, age progress in the selected Town Center panel, and rally point feedback.
- Controls: added `H` for Town Center select/center, `.` for next idle worker, `B` for the worker build menu, and smarter `Esc` behavior for placement/build menu cancellation.
- Tests: added core regressions for objective ordering, idle/gathering counts, queued population/rally production, and expanded the Playwright smoke path to cover objectives, TC production queue, rally point, age-up, Settlement build unlocks, and 980px HUD layout. Verified with `npm run build`, `npm run test`, and `npm run playtest`; screenshots are in `test-results/rts-core-loop-*.png`.
- Wall Builder v2: replaced one-line wall dragging with an AoE-style multi-click wall draft. Wall mode now supports visible turn previews, a HUD confirm/cancel dock, `Enter` confirmation, right-click/Esc cancellation, and one atomic `buildWallPath` command. Verified with `npm run build`, `npm run test`, `npm run playtest`, and screenshots `test-results/wall-builder-polyline-preview.png` / `test-results/wall-builder-polyline-built.png`.
- Visual direction pivot: accepted the Sunnyside/chibi asset direction and stopped relying on generated terrain atlases for the playable viewport. Added Sunnyside world asset preload, rendered the map with the `sunnyside_16.png` tileset through a Phaser Tilemap, replaced natural resource/decor visuals with Sunnyside elements, cropped several completed building sprites from the tileset, and shifted the HUD palette toward a softer pixel-game frame. The added `.aseprite` file is confirmed as a 96x64 human animation source with 310 frames, useful later for unit customization rather than terrain. Verified with `npm run build`, `npm run test`, `npm run playtest`, and screenshots `test-results/sunnyside-skin-initial.png`, `test-results/rts-core-loop-initial.png`, `test-results/rts-core-loop-build-menu.png`, and `test-results/rts-core-loop-980.png`.
- Sunnyside cleanup pass: replaced top-bar food/wood/stone icons with wheat/log/rock world elements, removed wheat ears and log props from the map layer, limited food nodes to carrots, changed water selection so light water stays on shore and deeper water stays inside, replaced the white building bracket selector with Sunnyside selectbox corners, and changed the House visual to a reduced full building crop instead of the narrow tower crop. Verified with `npm run build`, `npm run test`, `npm run playtest`, and screenshots `test-results/sunnyside-cleanup-wide.png` and `test-results/sunnyside-cleanup-towncenter-selected.png`.
- Sunnyside building FX pass: construction sites now render the finished building sprite as a transparent ghost with pulsing Sunnyside corner cursors, completed buildings get a short bounce effect, Town Center/House/camp-style buildings get chimney-smoke idle animation, and food bushes now use isolated fruit/flower frames from `sunnyside_16.png` instead of carrot props. Verified with `npm run build`, `npm run test`, `npm run playtest`, and screenshots `test-results/sunnyside-effects-final-initial.png` / `test-results/sunnyside-effects-final-construction.png`.
- Sunnyside building crop follow-up: removed the runtime dirt/ground tile sprites under completed buildings and Sunnyside placement previews, tightened the atlas rectangles for Town Center/House/Barracks/camps so they no longer include detached atlas fragments, and mapped economy camps to smaller dedicated atlas objects where available. Verified with `npm run build`, `npm run test`, `npm run playtest`, and screenshots `test-results/sunnyside-building-crops-final-initial.png` / `test-results/sunnyside-building-crops-final-house.png`.
- Sunnyside V2.1 inventory/blueprint pass: generated local atlas inspection sheets under `test-results/sunnyside-atlas-region-*.png`, `test-results/sunnyside-components-large.png`, and `test-results/sunnyside-building-crop-candidates.png`; switched completed/placement building rendering from one crop to small multi-sprite blueprints using V2.1 parts for Town Center, House, camps, Barracks, Watch Tower, and Farm. Verified with `npm run build`, `npm run test`, `npm run playtest`, and inspected `test-results/rts-core-loop-initial.png`, `test-results/rts-core-loop-build-menu.png`, and `test-results/sunnyside-blueprint-construction.png`.
- Tooling note: the `develop-web-game` Playwright client can read `render_game_to_text` state on this project, but its current screenshot path captures a black canvas because it uses `canvas.toDataURL` before falling back; use the repo Playwright screenshots for visual validation unless the client capture helper is patched to force `page.screenshot`.
- Hybrid terrain recovery: split the Sunnyside rendering flag into terrain vs building flags. The playable viewport now uses the older Solana/AOB terrain decals and continuous grass/water base again, while keeping Sunnyside buildings, unit animations, selection corners, smoke, and HUD icons. This removes the harsh 16px green tile-grid look without throwing away the useful Sunnyside building/animation work. Registered Sunnyside atlas building frames independently from the terrain path so buildings render correctly on the Solana terrain. Updated the Playwright visual path timeout and final build-menu reopen step to stay stable with heavier map screenshots. Verified with `npm run build`, `npm run test`, `npm run playtest`, and inspected `test-results/rts-core-loop-initial.png` / `test-results/rts-core-loop-980.png`.
- AOB asset rollback: disabled Sunnyside building blueprints and restored the existing AOB/Solana static building sprites, resource nodes, trees, bushes, and village props. Kept Sunnyside only for human unit animations, UI selection corners, and current HUD icons. Building ground pads, chimney smoke, and completion bounce are disabled for this AOB asset mode to avoid mixed-style effects. Verified with `npm run build`, `npm run test`, `npm run playtest`, and inspected `test-results/rts-core-loop-initial.png` / `test-results/rts-core-loop-980.png`.
- Cursor/assets check: wired Sunnyside `cursor_01.png` through `cursor_05.png` into Phaser cursor states for default, pointer, placement, construction, and attack/rally targeting. Inspected `public/Tiny RPG Character Asset Pack v1.03 -Free Soldier&Orc`; it contains 100x100 Soldier/Orc sheets plus 32x32/100x100 arrow projectiles, with Soldier and Orc idle/walk/hurt/death/attack strips. No cavalry asset was visible in that folder. Verified with `npm run build`, `npm run test`, and `npm run playtest`.
- Tiny RPG full-pack pass: detected `public/Tiny RPG Character Asset Pack v1.03 -Full 20 Characters`, switched the current Barracks infantry visual to the full-pack `Knight Templar` sheets, added `Archer` as a second Settlement-age Barracks unit with ranged combat stats, preloaded the full-pack archer sheets and arrow projectile asset, and wired Tiny RPG combat animations through the Phaser renderer/HUD. Added a core regression for Barracks archer production and range. Verified with `npm run build`, `npm run test`, and `npm run playtest`.
- Combat feedback/HUD pass: added core `combatEvents` so renderer effects can react to attacks without moving combat rules into Phaser. Archer attacks now emit arrow events and render flying arrow projectiles plus impact/damage feedback. Generated local pixel portraits from existing Sunnyside/Tiny RPG sheets under `public/assets/ui/portraits`, then updated selection panels, production queues, and unit command buttons to use those portraits with a more carved RTS panel treatment. Added a core regression for archer arrow combat events. Verified with `npm run build`, `npm run test`, and `npm run playtest`; latest screenshots include `test-results/rts-core-loop-980.png` and `test-results/rts-core-loop-towncenter-queue.png`.
- Building lifecycle pass: audited the current building assets and confirmed the playable building roster has static T1/T2/T3 assets for Town Center, House, Farm, Mill, Wood/Stone/Gold camps, Barracks, and Watch Tower. Added `cancelConstruction` and `destroyBuilding` commands, HUD Structure actions, and Delete-key handling for selected non-Town-Center buildings. Unfinished construction refunds the unbuilt cost and releases assigned builders; completed House destruction updates population cap. Added core regressions for cancel/refund and completed-building destruction. Verified with `npm run build`, `npm run test`, and `npm run playtest`.
- Ruin asset pass: processed the generated `raw-ruins.png` sheet into alpha-clean `ruins-small.png`, `ruins-medium.png`, and `ruins-large.png` under `public/assets/aob-buildings/ruins`. Added building destruction events to core state and mapped completed building destruction to persistent ruin visuals in Phaser, with small/medium/large sizing by building footprint. Extended Playwright to destroy the starter House and capture `test-results/building-ruin-house.png`. Verified with `npm run build`, `npm run test`, and `npm run playtest`.
- RTS solo loop follow-up: added villager repair commands/tasks, a Stable building unlock, `Lancer Scout` production from the Tiny RPG full pack, simple exploration/fog-of-war state, hidden unexplored neutral/enemy minimap dots, and new guided objectives for Stable, Scout, and map exploration. The objective HUD now shows a compact moving window around the current objective so the build dock does not overlap it on desktop or 980px. Added core regressions for repair, Stable/Scout production, visibility reveal, and the expanded objective chain; Playwright now checks the Stable unlock and objective/dock vertical gap. Verified with `npm run build`, `npm run test`, `npm run playtest`, and inspected `test-results/rts-core-loop-initial.png`, `test-results/rts-core-loop-build-menu.png`, and `test-results/rts-core-loop-980.png`.
- Wall UX correction: wall line generation now uses exact final segment lengths instead of always overshooting to 4-tile chunks, so turns stop on the clicked tile and do not continue past the corner. Wall mode now supports right-click to build the committed line and immediately choose a new start, plus a `New Start` HUD action to clear the current draft without exiting wall mode. Construction selection brackets are hidden for wall segments, and palisade visuals remain active across ages until dedicated stone/reinforced wall art exists. Added a core no-overshoot regression and updated Playwright to validate right-click wall confirmation. Verified with `npm run build`, `npm run test`, `npm run playtest`, and inspected `test-results/wall-builder-polyline-preview.png` / `test-results/wall-builder-polyline-built.png`.
- Wall drag follow-up: replaced the awkward multi-click/right-click wall validation flow with left-click drag placement. In wall mode, holding left click starts a preview, dragging defines the wall line, and releasing builds it while staying in wall mode for the next line. Added a HUD `Direction: Auto/Horizontal/Vertical` toggle so the player can force an axis when the automatic drag axis is not desired; right-click now cancels the current preview or exits wall mode instead of validating construction. Updated Playwright to validate drag preview, release-to-build, and vertical orientation lock. Verified with `npm run build`, `npm run test`, `npm run playtest`, inspected `test-results/wall-builder-polyline-preview.png` / `test-results/wall-builder-polyline-built.png`, and ran the develop-web-game client smoke path with no console error files under `test-results/web-game-client-wall-drag`.
- Public asset cleanup: moved generated/source-only Solana batches, cleaned legacy PNGs, map atlases, building source sheets, and archive generations under `public/_source`; moved the actively loaded terrain PNGs from `public/new-map` to `public/assets/aob-map/terrain`; updated preload paths and generation scripts so runtime assets still build from the new source locations. Added `public/_source/README.md` to document where future raw BTC/Solana imports should land. Verified source generators, `npm run build`, `npm run test`, and `npm run playtest`.
- BTC exploration village pass: moved the new BTC image drop from `public/btc` into `public/_source/btc-generated/raw`, added `scripts/promote-btc-assets.ps1` to crop and promote the usable transparent assets, and wired only the accepted BTC wall/gate pieces while leaving the incoherent corner/vertical wall files as source-only. Expanded the map to 192x160, added an eastern BTC village as fog-hidden exploration content, and captured `test-results/btc-village-exploration.png`. Verified with `npm run build`, `npm run test`, and `npm run playtest`.
- BTC discovery objective: added a `BTC Village` guided objective after the Stable/Scout step, completing when the scout reveals the BTC settlement tile and emitting a short discovery message. Exposed the discovery flag through the Playwright debug state and updated the visual smoke test to assert the BTC village can be revealed. Verified with `npm run build`, `npm run test`, and `npm run playtest`.
